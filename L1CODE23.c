#include <stdio.h>
int main() {
    float s1=80, s2=90, s3=70;
    float total = s1 + s2 + s3;
    float avg = total / 3;
    printf("Total = %.2f\n", total);
    printf("Average = %.2f", avg);
    return 0;
}
